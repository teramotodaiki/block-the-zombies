このリポジトリで AI コーディングを行うエージェントが最初に読むべきインデックスです。

## 目的

- このプロジェクトの目的・制約・ディレクトリ構成・ドキュメントの場所を一箇所に集約する。
- AI コーディング時に「どのドキュメントを参照すべきか」を明示する。
- 生コードやアセットから独立した「設計のソース・オブ・トゥルース」を持つ。

## 読む順番のガイド

1. `/docs/gdd.md`
   ゲームの概要・狙い・対象プレイヤーを理解する。
2. `/docs/game-rules.md`
   村人・ゾンビ・ブロック・勝敗条件など、ゲームルールの詳細を理解する。
3. `/docs/project-structure.md`
   ディレクトリ構成・ツールチェーンを確認し、どこに何を書くべきか把握する。
4. `/docs/architecture.md`
   `core` と Phaser など、レイヤ分離の方針を理解する。
5. `/docs/scenes.md`
   ゲームのシーン遷移（タイトル〜プレイ〜クリア）を把握する。
6. `/docs/levels-spec.md`
   レベルデータがどんな情報を持つか（抽象的な構造）を理解する。
7. `/docs/levels-authoring-guide.md`
   レベルを TypeScript で記述するときのルールを把握する。
8. `/docs/ui-layout.md`
   テキストなし UI のレイアウトとルールを確認する。
9. `/docs/input-spec.md`
   タップ入力→ゲーム内操作へのマッピングを理解する。
10. `/docs/visual-audio-spec.md`
    アートスタイル、アセットの粒度、AI 生成素材の扱い方を理解する。
11. `/docs/levels-autogen.md`
    レベル自動生成を行うときの、プロンプト設計と制約を確認する。
12. `/docs/testing-strategy.md`
    どこまでテストを書くべきか、何を保証するかを理解する。

## 重要な制約（要約）

- 画面内に **テキストは一切出さない**。
  UI はすべてアイコンや図形、補助として SE/BGM のみで伝える。
- 操作は **タップのみ**。長押しは使ってよい。
- ロジック層は **ゲームエンジン非依存** とし、Phaser とは薄いインターフェイスでつなぐ。
- レベルは **TypeScript ファイルとして記述** する（JSON ではない）。
- AI 生成素材は「生成データ置き場」と「ゲーム素材置き場」を分けて管理する。

## ドキュメントの更新ポリシー

- ドキュメントには具体的なコード・型定義は **書かない**。
  理由：コードは頻繁に変わるため、ドキュメントと乖離しやすいから。
- ディレクトリ構成・ツールチェーン・抽象的な構造・ルール・理由のみを書く。
- 未定の事項は **TBD** と明示する。

## デバッグ機能

- グローバル変数 `window.gameDebug` を介して、コンソールからゲームの状態を操作・確認できます。
- ブラウザテストや動作確認時には、クリック操作の代わりにこれらを使用することを推奨します。
- `src/global.d.ts` の型定義を参照してください。

## ツールチェーン

- Package Manager: pnpm
- Bundler: Vite
- Linter: Biome
- Formatter: Biome
- Framework: Phaser

## Workbench

ルートディレクトリにある `/workbench` ディレクトリは、ユーザーが用意した素材ファイルなどを置く場所です。そのままでは利用できないため、背景透過、トリミング、リサイズなどの編集をしてから public ディレクトリに移して利用してください。

`/workbench` は gitignore されています。

## git

- git commit はエージェントが適切な粒度で行うこと。ユーザーの指示を待つ必要はありません。
- 今はまだ remote repository がないので、 main branch に直接 commit してください。

## 画像生成

Nano Banana Pro 3 は透過色を生成できません。
白背景にしてから、背景を透過してください。
