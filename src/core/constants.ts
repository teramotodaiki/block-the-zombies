export const TILE_SIZE = 48;

// Physics constants (adjust as needed during tuning)
export const GRAVITY = 800; // pixels per second squared
export const MOVE_SPEED = 100; // pixels per second
export const JUMP_FORCE = 300; // pixels per second (if we had jumping, but for climbing it might be different)

// Game rules
export const MAX_FALL_SPEED = 600;
